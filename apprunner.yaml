version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt -t /app/deps
run:
  command: python3 -m uvicorn main:app --host 0.0.0.0 --port 3001 --log-level info
  network:
    port: 3001
  env:
    - name: PYTHONPATH
      value: /app/deps
